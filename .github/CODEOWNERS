# Кто автоматически назначается ревьюером. Строки ниже — не право вето (ревью не
# обязательно), а гарантия, что изменения в границе плагина и в системе сборки не
# проедут мимо глаз: их видят все, кто уже собрал GRAFT у себя.

*                    @6wingSerap

# Публичный API: всё, что попадает внутрь чужого плагина.
/INCLUDE/graft/      @6wingSerap

# Контракт хост↔плагин. Смена чисел здесь пересобирает всем всё — см. CONTRIBUTING, §5.
/INCLUDE/graft/abi.h @6wingSerap

# Система сборки: graft_import и graft_plugin() исполняются в чужих проектах.
/cmake/              @6wingSerap

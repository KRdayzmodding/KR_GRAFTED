// Пусто — и это главное, что тут можно сказать.
//
// Раньше здесь стояла строка `GraftTick(timeslice, GetGame())` из OnUpdate миссии: C++
// иначе было не разбудить, а корень объектного графа взять неоткуда. Теперь библиотека
// берёт кадр у самого движка — перенаправляя вызов скриптового OnUpdate внутри
// CGame::Update, — и корень приезжает первым аргументом. См. README, «Точка входа».
//
// Файл оставлен пустым нарочно: пример должен показывать, что на стороне скрипта писать
// НЕЧЕГО, а удалённый файл этого не показывает.

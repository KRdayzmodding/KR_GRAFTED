#include "graft/native.hpp"

// Паспорт плагина: имя, под которым он представляется хосту, и своя версия. Ровно один
// раз на плагин. Имя должно совпадать с NAME в graft_plugin() — от него зависит и имя
// файла DLL, и суффикс служебного натива NativeDispose_*.
GRAFT_PLUGIN("EXAMPLE_GRAFT", 1);

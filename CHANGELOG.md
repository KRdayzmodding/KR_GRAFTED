# Изменения

Формат — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
версионирование — [semver](https://semver.org/lang/ru/).

До `1.0` ломающие изменения выходят в **минорной** версии.

Каждый релиз обязан нести две строки: **сборка DayZ, на которой проверено**, и **версии
ABI**. Плагин, собранный под другой `ABI`, хост не загрузит — это не сюрприз, это
проверка в `SRC/graft/plugins.cpp`.

## [Unreleased]

Проверено на: —
ABI: 5, LAYOUT: 2

### Изменено

- Вендоренный **KR_UTEST** переименовал все свои имена под префикс `KRU_`
  (`KRU_Suite`, `KRU_Case`, `KRU_TEST_SUITE`, `KRU_TEST_CASE`, `KRU_Runner`,
  `KRU_Status`, `KRU`). Раньше они совпадали с проприетарным uTest из `KR_CORE`, и два
  определения одних и тех же классов не компилировались — тестовый мод нельзя было
  загрузить рядом с `KR_CORE` без `-scrDef=UTESTS`. Теперь можно, флаг больше не нужен.
  Сьюта `seraph::graft` переписана под новые имена, поведение и вывод те же.

---

## [0.1.0] — 2026-08-20

Первая версия с тегом: до неё пользоваться graft можно было только с движущегося `main`,
а значит бамп ABI ломал всех разом и без предупреждения. С этого релиза мод пинится на
тег — `graft_import(... TAG v0.1.0)`.

Проверено на: **DayZ 1.29.0.163709** (клиент и `DayZDiag_x64`)
ABI: 5, LAYOUT: 2

### Добавлено

- Инфраструктура открытого проекта: `CONTRIBUTING.md`, `SECURITY.md`,
  `CODE_OF_CONDUCT.md`, соглашение о вкладе (`docs/CLA.md`) с подписью ботом, шаблоны
  issue и PR, CI на GitHub Actions (формат, сборка, тесты, вентиль ABI), `CODEOWNERS`,
  `dependabot`.
- Каждый пуш в `main` кладёт `dwmapi.dll` и `graft.exe` в черновик пред-релиза
  `v<версия>-rc.N` — скачать сборку можно, не собирая ничего и не логинясь.
- `.gitattributes`: в репозитории LF, у Windows-скриптов на диске CRLF.
- В `CONTRIBUTING.md` — как править graft прямо из своего мода
  (`-DGRAFT_SOURCE_DIR`) и что считается недопустимым следом AI-ассистента.

### Изменено

- Скриптовая сьюта переехала с uTest из `KR_CORE` на **KR_UTEST** — фреймворк лежит
  копией в `tests/mod/SIXW_GRAFT/scripts/3_Game/uTest.c` (MIT, один файл). Тестовый мод
  больше не зависит ни от `KR_CORE`, ни от Community Framework: собрать и прогнать его
  может любой, у кого есть игра. Кейсы стали обычными `void Имя()` вместо корутин,
  запуск — `-scrDef=UTESTS_RUN -utest=seraph::graft`.
- README открывается модом целиком: C++, сгенерированное объявление и скрипт мода —
  три файла подряд. Прежний пример показывал функцию, которой в репозитории не было.
- `graft_import` в README пинится на `TAG v0.1.0` вместо `main`.

---

## [0.0.1] — не выпускалась

Состояние репозитория до открытия проекта: библиотека, `graft.exe`, хост `dwmapi.dll`,
тестовое окружение (юнит-тесты + скриптовая сьюта на 88 кейсов), четыре примера, база
знаний по движку в `RESEARCH/`.

ABI: 5, LAYOUT: 2

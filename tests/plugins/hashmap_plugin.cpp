#include "graft/native.hpp"

// ВТОРОЙ плагин этого репозитория — он существует ради проверки, а не ради удобства.
// Шаблонная хеш-таблица вынесена из основного плагина в отдельную DLL, поэтому сьюта
// seraph::graft гоняет два плагина в одном процессе: кейсы HashMap_* работают через
// этот, всё остальное — через SIXW_GRAFT. Если сосуществование сломается, покраснеет
// обычный прогон, а не отдельный ритуал, который забудут запустить.
GRAFT_PLUGIN("SIXW_HASHMAP", 1);
